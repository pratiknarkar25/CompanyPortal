<%= form_with(model: category, local: true) do |form| %>
  <% if category.errors.any? %>
    <div id="error_explanation" class='form-group col-md-8'>
      <h3><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h3>
      <ul>
      <% category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class='form-group col-md-8'>
    <%= form.label :name %>
    <%= form.text_field :name, id: :category_name, class: 'form-control' %>
  </div>

  <div class='form-group col-md-8'>
    <%= form.label :description %>
    <%= form.text_area :description, id: :category_description, class: 'tinymce' %>
  </div>

  <div class='form-group col-md-8'>
    <%= form.submit "Submit", class: "btn btn-default" %>
  </div>
<% end %>
<%= tinymce %>
